import SwiftUI

@main
struct ToggleApp: App {
    var body: some Scene {
        Window("Toggle 开关演示", id: "main") {
            ContentView()
        }
        .defaultSize(width: 450, height: 400)
    }
}
